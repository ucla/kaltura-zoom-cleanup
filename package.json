{
  "name": "kaltura-zoom-cleanup",
  "license": "BSD-2-Clause",
  "scripts": {
    "deploy": "yarn --production && sls deploy --stage=prod --verbose && yarn",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "local": "sls invoke local -f cleanup"
  },
  "dependencies": {
    "@jwerre/secrets": "^0.2.2",
    "kaltura-client": "^16.5.0",
    "sleep-promise": "^8.0.1"
  },
  "devDependencies": {
    "eslint": "^7.6.0",
    "eslint-config-airbnb-base": "^14.2.0",
    "eslint-plugin-import": "^2.22.0"
  }
}
