{
  "name": "kaltura-zoom-cleanup",
  "license": "BSD-2-Clause",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "deploy": "yarn --production && sls deploy --stage=prod --verbose"
  },
  "dependencies": {
    "@jwerre/secrets": "^0.2.2",
    "kaltura-client": "^16.5.0"
  },
  "devDependencies": {
    "eslint": "^7.6.0",
    "eslint-config-airbnb-base": "^14.2.0",
    "eslint-plugin-import": "^2.22.0"
  }
}